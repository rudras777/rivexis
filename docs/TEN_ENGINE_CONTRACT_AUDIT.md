# Rivexis Ten-Engine Contract Audit

Last updated: 2026-09-23

This audit distinguishes repository implementation gaps from external provider, credential, licensing and customer-contract gates. A missing commercial credential is not treated as a code defect; the engine must instead remain explicit about unavailable or partial evidence.

| Engine | Current live version | Grounding currently implemented | Safe live state / known gap |
|---|---:|---|---|
| B1 — Transaction Simulation | 1.3.0 | EVM RPC state/dry-run; mined transaction receipts for historical hashes; optional `debug_traceCall` call tree/prestate diff; Tenderly simulation/event logs when configured; optional verified ABI lookup | Fresh live outputs include a deterministic `transaction_effects` summary plus canonical standard ERC-20/ERC-721/ERC-1155 transfer/approval effects when raw logs are available. Mined receipt logs are observed evidence and take precedence over replay/simulation logs; Tenderly logs are predicted evidence for prospective simulations. Standard-event parsing now requires canonical ERC-1155 batch ABI layout, preserves zero log indexes and bounds normalization to 1,000 input logs with explicit omitted/truncated coverage. Raw amounts/token IDs stay unscaled and unknown/malformed/non-standard logs remain explicit. Revert remains a material blocker; deeper internal/state/security semantics keep the live result `PARTIAL`. |
| B2 — Transaction & Contract Security | 1.1.0 | EVM RPC bytecode/calldata rules; optional Etherscan; Blockaid when resolved | EVM target/sender addresses and transaction hashes are validated as actual hexadecimal widths before provider use, and transaction addresses resolved from RPC are revalidated before security analysis. Invalid input remains `INSUFFICIENT_DATA`/UNKNOWN rather than being mislabeled as provider outage. Successful live analysis stays `PARTIAL` when external threat intelligence is absent; Hypernative customer-only request schema remains explicitly gated rather than invented. |
| B3 — Threat & Monitoring | 1.1.0 | Validated EVM RPC point-in-time snapshots; balance/code/supply change detection; optional Chainlink state and configured threat provider | Prior-snapshot identity must match entity/network/dependencies before comparison. Block, balance, bytecode and token-supply state are validated instead of becoming zeros or crashes; monitoring thresholds must be finite/economically valid before provider access. Chainlink future timestamps are `UNKNOWN`, stale/expired or policy-aged observations promote the result to `STALE_DATA`, and malformed prior snapshot fields suppress the affected delta rather than fabricating signals. Snapshot monitoring still remains point-in-time; continuous provider-native threat/event coverage is not fabricated. |
| B4 — Entity & Fund Flow | 1.0.0 | Direct wallet state; optional Etherscan indexed history; optional Nansen; Arkham only when license-approved | RPC/indexed numeric state is validated; malformed history rows are excluded from activity scoring. ERC-20 flow identity is keyed by normalized contract address rather than ticker symbol, conflicting decimals for the same contract are skipped explicitly, and directional counterparty/native/token flow plus interaction concentration is exposed from validated indexed records. Concentration is descriptive-only and is not treated as economic exposure, ownership or maliciousness. External indexer/label evidence without normalized provider observation time remains `UNKNOWN`; `UNKNOWN ADDRESS` and unresolved `CONFLICTING_DATA` identity states remain explicit. |
| B5 — Cross-Chain Route | 1.2.0 | LI.FI route quote normalization plus request/response integrity validation | Caller amount/address inputs are validated before provider use. Returned chain IDs, token identities, amount, addresses and slippage must match the requested route, and `toAmount`/`toAmountMin` must be internally valid. Any mismatch becomes `CONFLICTING_DATA` with UNKNOWN severity and route scoring is discarded. A matching quote remains `PARTIAL` because route-aggregator evidence is not an independent bridge-security/liquidity/incident assessment. |
| F1 — Portfolio & Exposure | 1.2.0 | CoinGecko market references; direct native-wallet balances; direct `balanceOf(address)` reads for caller-declared ERC-20 contracts; user-declared manual positions | Manual quantities must be finite/non-negative; direct block/native/token quantities must be valid non-negative EVM quantities; duplicate wallets and exact duplicate token descriptors are deduplicated while conflicting token metadata fails closed. If any explicitly requested wallet/token read fails, F1 does not score the remaining subset. Every positive exposure must have a positive finite CoinGecko price before concentration/HHI scoring, so an unpriced exposure cannot silently disappear from weights. Market freshness is `UNKNOWN` when any requested asset timestamp is missing/invalid/materially future. Automatic token discovery, NFTs and DeFi positions remain explicit missing coverage. |
| F2 — Protocol Risk | 1.2.0 | DefiLlama fundamentals plus optional direct protocol-native/adapter evidence | DefiLlama records must have a usable protocol identity; non-object/identityless payloads and non-finite or negative core TVL fail closed instead of receiving a score. TVL-history timestamps drive evidence freshness when credible; absent/unusable observation time remains `UNKNOWN`, and stale/expired TVL or `latestFetchIsOk=false` promotes the result to `STALE_DATA`. Independent exploit, governance, liquidity and security coverage remains explicit and otherwise successful screening stays `PARTIAL`. |
| F3 — Position & Liquidation Risk | 1.3.0 | Protocol-specific adapter path where available; otherwise caller-modeled position with direct oracle evidence and optional CoinGecko/protocol-native checks | `PARTIAL`, `STALE_DATA` or `CONFLICTING_DATA` as applicable. Explicit `protocol_adapter + user/wallet` requests fail closed to `INSUFFICIENT_DATA`/`PROVIDER_UNAVAILABLE` unless an authoritative adapter position is actually returned; generic modeled fallback conclusions are discarded for that explicit request. |
| F4 — Yield & Strategy Risk | 1.2.0 | Attributed DefiLlama yield-pool evidence plus optional protocol-native evidence | Broad selectors must identify exactly one current pool; Rivexis no longer silently chooses the largest-TVL candidate when multiple pools match. Missing/non-finite core APY or TVL metrics fail closed instead of becoming zero values. Stale/expired pool timestamps promote the result to `STALE_DATA`, while materially future timestamps are `UNKNOWN` freshness. Principal/security/oracle/withdrawal gaps remain explicit and otherwise successful screening stays `PARTIAL`. |
| F5 — Treasury Allocation & Scenario | 1.2.0 | User allocation model; CoinGecko market references where requested; optional protocol-native checks | Quantity, weight, capital and policy/scenario percentages must be finite and economically valid; `stablecoin` must be a real JSON boolean. Derived weights require complete positive valuation for every allocation, so missing market data cannot silently create zero-weight exposures. Stale/expired market references promote the result to `STALE_DATA`. Dependency/correlation/liquidity/counterparty evidence remains explicit and successful live screening stays `PARTIAL`. |

## Cross-engine contract rules

For newly executed live runs, the dispatcher enforces a canonical current engine version for each engine. Evidence records inside the run inherit that parent engine version while preserving a more specific shared/provider calculation version when one exists. This avoids a result claiming one methodology generation while its evidence claims an obsolete/default parent engine generation.

Provider consensus is derived from evidence actually present in the result: zero provider evidence cannot claim `SINGLE_SOURCE`; one provider is `SINGLE_SOURCE`; multiple providers are `MULTI_SOURCE`; unresolved conflicts are `CONFLICTING`. `USER_INPUT_ONLY` is preserved only for intentionally modeled results with no provider evidence.

Unresolved provider conflicts are also first-class analysis state. A fresh live result carrying `SourceConflict` records is promoted from ordinary `COMPLETED`/`PARTIAL` to `CONFLICTING_DATA`; `STALE_DATA` and terminal unavailable/failure states are preserved when they are the stronger primary gate.

The normalization is intentionally limited to **new live dispatches**. Historical persisted analyses are not rewritten because their stored versions are part of the audit record.

For F3, an explicit protocol-adapter request is a trust-boundary instruction. If that adapter path does not return an authoritative position, Rivexis does not silently substitute the generic modeled-oracle conclusion. It returns an explicit unavailable/insufficient state with zero confidence and UNKNOWN severity. The generic modeled path remains available only when the caller does not require an authoritative adapter result.

For B1, the canonical transaction-effects summary is a derived view of evidence already present in the result. Call traces remain useful for internal calls/native value/approval candidates but are not treated as proof of token/NFT movement. Standard ERC-20/ERC-721/ERC-1155 transfer and approval effects are normalized only from raw event logs whose full standard topic signature and ABI layout validate. For a supplied mined transaction hash, `eth_getTransactionReceipt` logs are observed evidence and take precedence over replay/simulation logs. For prospective Tenderly simulations, available raw logs are predicted evidence. Event amounts and token IDs remain raw integers; decimals, symbols, prices, balances and ownership are not inferred beyond the standard emitted event. ERC-1155 batches require canonical dynamic-array offsets and exact ABI length. Event-log normalization is bounded to 1,000 input logs; omitted and output-truncated coverage is surfaced rather than silently discarded.

For F1, the portfolio input/evidence set is a scoring boundary. Manual quantities must be finite and non-negative. Explicit wallet lists are validated and deduplicated before reads; block/native/ERC-20 balances must normalize to non-negative EVM quantities and malformed quantities never become zero exposure. Exact duplicate token descriptors are deduplicated, but the same contract with conflicting asset identity or decimals is invalid input. If any explicitly requested wallet/token read fails, Rivexis returns an unavailable state instead of scoring a subset. After economic exposures are aggregated, every positive exposure must receive a positive finite market price before value, weight, HHI or concentration scoring; missing/non-finite prices fail closed instead of silently removing that exposure. Market-reference freshness is derived only when every requested asset has a usable non-future observation timestamp. Direct ERC-20 identity/decimals/CoinGecko mapping remain caller metadata, and automatic token/NFT/DeFi discovery remains an explicit gap.

For B5, the caller's route request is the trust-boundary contract. LI.FI quote fields are checked against that request before risk scoring. A route with mismatched source/destination chain, token, amount or addresses, a changed quoted slippage, a malformed expected/minimum amount, or a minimum amount larger than the expected amount is retained as evidence but cannot become an ordinary scored route. It is returned as `CONFLICTING_DATA` with UNKNOWN severity so downstream decisioning fails closed. A quote that matches its request is still only aggregator evidence and remains `PARTIAL` until independent bridge/security/liquidity evidence exists.

For B2, malformed target/sender addresses and transaction hashes are rejected before provider calls. When a transaction hash resolves, its sender/destination are revalidated before bytecode lookup or external security scanning. This prevents malformed caller/provider data from being mislabeled as an RPC/security-provider outage and prevents invalid address strings from crossing the external-provider trust boundary.

For B3, both the current snapshot and the prior comparison state are trust boundaries. Current block/balance/bytecode/total-supply quantities must normalize as valid non-negative EVM state; malformed data never becomes a zero baseline. Monitoring thresholds are validated as finite policy values before provider use. Chainlink `latestRoundData()` must have a valid exact return shape, bounded decimals and a usable timestamp; materially future timestamps are `UNKNOWN`, while stale/expired evidence or a caller policy age breach promotes the analysis to `STALE_DATA`. Prior balance/supply/oracle/hash fields are validated before their specific delta check, and malformed fields reduce confidence/coverage rather than triggering fabricated withdrawal, supply, oracle or bytecode-change signals. Prior snapshot entity/network/dependency identity validation still runs before live comparison.

For B4, direct-chain and external-provider evidence are kept distinct. Malformed RPC numeric state cannot become a zero block/balance, malformed indexed amounts/decimals are skipped with an explicit coverage gap, and malformed identity-provider payloads are not converted into benign no-attribution evidence. ERC-20 asset identity is contract-based; ticker symbols are descriptive metadata and cannot merge unrelated contracts. Conflicting decimals for one contract cause the offending row to be excluded. Activity thresholds, counterparty flow and concentration use only validated normalized records. Counterparty concentration is descriptive interaction concentration—not economic exposure, identity, ownership or maliciousness—and is not used in B4 risk scoring. A valid provider response containing no identity is still evidence that no attribution was returned, but only actual identity candidates raise attribution confidence. External evidence without normalized provider observation time remains `UNKNOWN` freshness.

For F2, a usable provider record is required before scoring. Non-object or identityless records and non-finite/negative core TVL are rejected rather than converted into plausible fundamentals. When the TVL history contains a credible provider timestamp, that timestamp—not HTTP retrieval time—drives freshness. Missing/corrupt observation time stays `UNKNOWN`; stale/expired observations and an explicit provider `latestFetchIsOk=false` state gate the analysis as `STALE_DATA` so downstream decisioning cannot treat degraded fundamentals as ordinary partial evidence.

For F4, a selector is a strategy-identity boundary. An exact `pool_id` must resolve uniquely; a broader protocol/project + asset/symbol + chain selector is accepted only when it resolves to exactly one current DefiLlama pool. Multiple candidates are returned as an explicit insufficient-selection state rather than silently selecting the largest TVL. APY and TVL are core scoring inputs and must be finite (with TVL non-negative); malformed values do not become zeros. Stale or expired provider timestamps are a first-class `STALE_DATA` gate, while materially future timestamps are not treated as current evidence.

For F5, treasury inputs are decision-model inputs and must be validated before scoring. Negative or non-finite weights/quantities, non-positive supplied capital, out-of-range policy/shock percentages and non-boolean `stablecoin` flags fail closed rather than being normalized into plausible-looking allocation metrics. When weights are derived from quantities, every allocation must have a usable market mapping and positive current price; Rivexis does not silently assign an unpriced allocation zero exposure. Stale or expired market timestamps are a first-class `STALE_DATA` gate.

## External gates that remain non-code blockers

- Arkham remains license/terms-gated.
- Hypernative native screening remains customer-schema/contract gated where the exact authenticated request contract is not public/approved.
- Tenderly, Blockaid, Nansen, explorer and RPC behavior depends on deployment configuration and valid credentials/entitlements.
- Absence of those integrations must remain explicit provider-unavailable/partial/UNKNOWN-compatible evidence, not synthetic success.

## Latest repository evidence

- PR #26 — B1 canonical standard event effects — full CI #271 (`35857083832`) PASS before merge.
- PR #27 — F1 numeric/completeness integrity — full CI #277 (`35861080306`) PASS before merge.
- PR #28 — B4 token identity and counterparty-flow integrity — full CI #282 (`35895021465`) PASS; merge `d8bb045800ee534d182acc88d094589bafa816a2`.
- PR #29 — B1 event parser ABI/input bounds — full CI #287 (`35896050913`) PASS; merge `0d56ba9ca80e340b21ea48f6f990aa7d04d77b3e`.
- PR #30 — B3 monitoring runtime integrity — full CI #293 (`35897012617`) PASS; merge `042f08376f3b27cbd87dec0df6fb72ed063cdb52`.

## Remaining Milestone F work

The repository-level cross-engine contract plus the integrity defects identified so far are CI-covered. Remaining work is capability depth rather than a known hidden build/test error: deeper B1 internal/state/security semantics beyond standard logs; deeper B2/B3 external threat/security evidence where an approved provider contract exists; B4 cross-chain and protocol-semantic enrichment beyond validated single-chain indexed history; F1 automatic/indexed token discovery plus NFT/DeFi positions from approved evidence sources; independent B5 bridge/security/liquidity/incident evidence; and deeper F2/F4/F5 dependency/liquidity/governance/counterparty coverage. Keep credential/licensing/customer-contract gaps explicit and add focused regression evidence before advancing to Milestone G.
