"use client";
import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {useQuery} from "@tanstack/react-query";import {Brand} from "./Brand";import {activeWorkspaceId,api,setActiveWorkspaceId,setCsrfToken} from "@/lib/api";import {useEffect,useState} from "react";
const nav=[['Overview','/workspace'],['Workspaces','/workspace/settings'],['Providers','/workspace/providers'],['Protocol History','/workspace/protocol-history'],['Investigations','/workspace/investigations'],['Monitors','/workspace/monitors'],['History','/workspace/history'],['Saved','/workspace/saved']];
type Workspace={id:string;name:string;role:string;organization_id?:string|null;access_role:string};
export function AppShell({children}:{children:React.ReactNode}){const p=usePathname();const router=useRouter();const [active,setActive]=useState<string>("");const [loggingOut,setLoggingOut]=useState(false);const q=useQuery({queryKey:["workspaces-shell"],queryFn:()=>api<{items:Workspace[]}>("/api/v1/workspaces")});useEffect(()=>{if(!q.data?.items.length)return;const stored=activeWorkspaceId();const chosen=q.data.items.find(w=>w.id===stored)?.id??q.data.items[0].id;setActive(chosen);setActiveWorkspaceId(chosen)},[q.data]);function change(id:string){setActive(id);setActiveWorkspaceId(id);window.dispatchEvent(new Event("rivexis-workspace-change"));window.location.reload()}async function logout(){if(loggingOut)return;setLoggingOut(true);try{await api("/api/v1/auth/logout",{method:"POST"});setCsrfToken(null);if(typeof window!=="undefined")localStorage.removeItem("rivexis_workspace_id");router.replace("/login");router.refresh()}catch{setLoggingOut(false)}}return <div className="appShell"><aside className="sidebar"><Brand/><div className="environment">MVP / WORKSPACE-SCOPED</div>{q.data?.items.length?<label style={{fontSize:11,display:"grid",gap:6}}>ACTIVE WORKSPACE<select value={active} onChange={e=>change(e.target.value)}>{q.data.items.map(w=><option key={w.id} value={w.id}>{w.name} · {w.access_role}</option>)}</select></label>:null}<nav aria-label="Workspace">{nav.map(([n,h])=><Link key={h} href={h} className={p===h?'active':''}>{n}</Link>)}</nav><div className="sideFoot"><button type="button" className="ghost" onClick={logout} disabled={loggingOut}>{loggingOut?"Logging out…":"Log out"}</button><b>Decision states</b><span>PROCEED · MODIFY · WAIT · AVOID · UNKNOWN</span></div></aside><main className="workspaceMain">{children}</main></div>}
