"use client";import {useForm} from "react-hook-form";import {useRouter} from "next/navigation";import {Brand} from "@/components/Brand";import {api} from "@/lib/api";
type Form={name:string;role:string};export default function Onboarding(){const {register,handleSubmit}=useForm<Form>({defaultValues:{name:"Primary Workspace",role:"Individual"}});const router=useRouter();async function submit(v:Form){await api('/api/v1/me/role',{method:'PATCH',body:JSON.stringify({role:v.role})});await api('/api/v1/workspaces',{method:'POST',body:JSON.stringify(v)});router.push('/workspace')}return <main className="formPage"><section className="formCard"><Brand/><h1>Configure workspace</h1><p>Your role changes defaults and information density, not the underlying ten-engine architecture.</p><form className="form" onSubmit={handleSubmit(submit)}><label className="field">Workspace name<input {...register('name')} required/></label><label className="field">Role<select {...register('role')}><option>Individual</option><option>Fund</option><option>Treasury</option><option>Analyst</option></select></label><button className="button">Open workspace</button></form></section></main>}
