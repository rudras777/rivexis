"use client";
import {useState} from "react";import {useMutation,useQuery,useQueryClient} from "@tanstack/react-query";import {activeWorkspaceId,api,setActiveWorkspaceId} from "@/lib/api";
type Workspace={id:string;name:string;role:string;organization_id?:string|null;access_role:string};type Org={id:string;name:string;member_role:string};
export default function Settings(){const qc=useQueryClient();const [name,setName]=useState("Institutional Workspace");const [role,setRole]=useState("Analyst");const [orgName,setOrgName]=useState("Rivexis Organization");const ws=useQuery({queryKey:["workspace-settings"],queryFn:()=>api<{items:Workspace[]}>("/api/v1/workspaces")});const orgs=useQuery({queryKey:["organizations"],queryFn:()=>api<{items:Org[]}>("/api/v1/organizations")});const makeWs=useMutation({mutationFn:()=>api<Workspace>("/api/v1/workspaces",{method:"POST",body:JSON.stringify({name,role})}),onSuccess:r=>{setActiveWorkspaceId(r.id);qc.invalidateQueries({queryKey:["workspace-settings"]});qc.invalidateQueries({queryKey:["workspaces-shell"]})}});const makeOrg=useMutation({mutationFn:()=>api<Org>("/api/v1/organizations",{method:"POST",body:JSON.stringify({name:orgName})}),onSuccess:()=>qc.invalidateQueries({queryKey:["organizations"]})});return <><div className="workspaceHeader"><div><h1>Workspaces & Organizations</h1><p>Access is enforced by the API. Organization roles are OWNER, ADMIN, ANALYST and VIEWER.</p></div><span className="badge">ACTIVE {activeWorkspaceId()??"AUTO"}</span></div><section className="panel"><h2>Your workspaces</h2><div className="tableWrap"><table className="table"><thead><tr><th>Name</th><th>Context</th><th>Access</th><th>Organization</th><th></th></tr></thead><tbody>{ws.data?.items.map(w=><tr key={w.id}><td>{w.name}</td><td>{w.role}</td><td>{w.access_role}</td><td>{w.organization_id??"Personal"}</td><td><button className="button" onClick={()=>{setActiveWorkspaceId(w.id);window.dispatchEvent(new Event("rivexis-workspace-change"));window.location.reload()}}>Use</button></td></tr>)}</tbody></table></div></section><section className="panel"><h2>Create personal workspace</h2><div style={{display:"grid",gridTemplateColumns:"2fr 1fr auto",gap:12,alignItems:"end"}}><label className="field">Name<input value={name} onChange={e=>setName(e.target.value)}/></label><label className="field">Role<select value={role} onChange={e=>setRole(e.target.value)}><option>Individual</option><option>Fund</option><option>Treasury</option><option>Analyst</option></select></label><button className="button" onClick={()=>makeWs.mutate()}>Create</button></div></section><section className="panel"><h2>Organizations</h2><pre className="result">{JSON.stringify(orgs.data?.items??[],null,2)}</pre><div style={{display:"flex",gap:12,marginTop:12}}><input value={orgName} onChange={e=>setOrgName(e.target.value)} style={{flex:1}}/><button className="button" onClick={()=>makeOrg.mutate()}>Create organization</button></div></section></>}
