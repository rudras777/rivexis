"use client";import {useState} from "react";import {useForm} from "react-hook-form";import {useRouter} from "next/navigation";import {Brand} from "@/components/Brand";import {api,setCsrfToken} from "@/lib/api";
type Form={email:string;password:string;role:string};
export default function Signup(){const {register,handleSubmit}=useForm<Form>({defaultValues:{role:"Individual"}});const [error,setError]=useState("");const router=useRouter();async function submit(v:Form){try{const r=await api<{csrf_token:string}>("/api/v1/auth/web/signup",{method:"POST",body:JSON.stringify(v)});setCsrfToken(r.csrf_token);router.push("/onboarding")}catch(e){setError(e instanceof Error?e.message:"Signup failed")}}return <main className="formPage"><section className="formCard"><Brand/><h1>Create workspace</h1><p>Choose the role that controls terminology, defaults and reporting depth.</p><form className="form" onSubmit={handleSubmit(submit)}><label className="field">Email<input type="email" required {...register("email")}/></label><label className="field">Password<input type="password" required minLength={8} {...register("password")}/></label><label className="field">Who are you?<select {...register("role")}><option>Individual</option><option>Fund</option><option>Treasury</option><option>Analyst</option></select></label>{error&&<div className="error" role="alert">{error}</div>}<button className="button">Continue</button></form></section></main>}
