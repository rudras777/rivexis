"""Single release/version source of truth."""

API_VERSION = "3.4.3"
RELEASE_CODENAME = "P37"

# Versioned analytical contracts are persisted with results/decisions so a later
# review can identify which shared Rivexis semantics were in force even when an
# individual specialist engine has its own independent engine_version.
ANALYSIS_FRAMEWORK_VERSION = "1.0.0"
DECISION_METHODOLOGY_VERSION = "1.1.0"
