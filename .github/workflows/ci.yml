name: ci
on: [push, pull_request]
jobs:
  api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.13'}
      - run: python -m pip install --upgrade pip setuptools
      - run: cd apps/api && pip install -e '.[dev,full]'
      - run: cd apps/api && ruff check rivexis_api tests
      - run: cd apps/api && pytest -q
      - run: cd apps/api && pip-audit --skip-editable
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22', cache: 'npm'}
      - run: npm ci
      - run: npm run typecheck:api-edge
      - run: npm run typecheck:web
      - run: npm run build:web
      - run: npm --workspace @rivexis/web run build:vinext
      - run: npm audit --audit-level=high
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
  invariants:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.13'}
      - run: python -m pip install -e './apps/api'
      - run: python scripts/check_invariants.py
      - run: python scripts/check_migrations.py
      - run: python scripts/secret_scan.py
  postgres-migrations:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: rivexis_ci
          POSTGRES_PASSWORD: ci_only
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres -d rivexis_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.13'}
      - run: python -m pip install -e './apps/api[postgres]'
      - name: Create non-bypass migration role
        env:
          PGPASSWORD: ci_only
        run: |
          psql -h 127.0.0.1 -U postgres -d rivexis_ci -v ON_ERROR_STOP=1 -c "CREATE ROLE rivexis_migrator LOGIN PASSWORD 'ci_only' NOSUPERUSER NOCREATEDB NOCREATEROLE NOBYPASSRLS"
          psql -h 127.0.0.1 -U postgres -d rivexis_ci -v ON_ERROR_STOP=1 -c "GRANT USAGE, CREATE ON SCHEMA public TO rivexis_migrator"
          psql -h 127.0.0.1 -U postgres -d rivexis_ci -v ON_ERROR_STOP=1 -c "CREATE ROLE rivexis_app LOGIN PASSWORD 'ci_only' NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION NOINHERIT NOBYPASSRLS"
          psql -h 127.0.0.1 -U postgres -d rivexis_ci -v ON_ERROR_STOP=1 -c "GRANT CONNECT ON DATABASE rivexis_ci TO rivexis_app; GRANT USAGE ON SCHEMA public TO rivexis_app"
      - name: Upgrade actual PostgreSQL schema
        working-directory: apps/api
        env:
          DATABASE_URL: postgresql+psycopg://rivexis_migrator:ci_only@127.0.0.1:5432/rivexis_ci
        run: python -m alembic upgrade head
      - name: Certify distributed PostgreSQL runtime controls
        env:
          DATABASE_URL: postgresql+psycopg://rivexis_app:ci_only@127.0.0.1:5432/rivexis_ci
        run: python scripts/certify_postgres_distributed_controls.py
      - name: Verify migrated PostgreSQL schema
        env:
          PGPASSWORD: ci_only
        run: |
          test "$(psql -h 127.0.0.1 -U rivexis_migrator -d rivexis_ci -tAc 'select version_num from alembic_version')" = '0012_postgres_performance_hardening'
          test "$(psql -h 127.0.0.1 -U rivexis_migrator -d rivexis_ci -tAc "select count(*) from pg_tables where schemaname='public' and tablename<>'alembic_version'")" = 55
          test "$(psql -h 127.0.0.1 -U rivexis_migrator -d rivexis_ci -tAc \"select count(*) from pg_constraint c where c.contype='f' and c.connamespace='public'::regnamespace and not exists (select 1 from pg_index i where i.indrelid=c.conrelid and i.indisvalid and i.indnkeyatts >= cardinality(c.conkey) and not exists (select 1 from generate_subscripts(c.conkey,1) s where i.indkey[s-1] <> c.conkey[s]))\")" = 0
          test "$(psql -h 127.0.0.1 -U rivexis_migrator -d rivexis_ci -tAc \"select count(*) from pg_indexes where schemaname='public' and indexname in ('ix_users_email','ix_workspaces_owner_user_id')\")" = 0
          test "$(psql -h 127.0.0.1 -U rivexis_migrator -d rivexis_ci -tAc \"select count(*) from pg_policies where schemaname='public' and tablename in ('workspaces','organizations','organization_members','profiles') and (position('NULLIF(current_setting' in coalesce(qual,'')) > 0 or position('NULLIF(current_setting' in coalesce(with_check,'')) > 0)\")" = 0
